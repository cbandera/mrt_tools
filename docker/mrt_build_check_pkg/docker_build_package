#!/bin/bash

set -e

# copy user data
cp /tmp/userFiles/gitconfig ~/.gitconfig
mkdir -p ~/.mrt_tools
cp /tmp/userFiles/mrt.cfg ~/.mrt_tools/

source /opt/ros/jade/setup.bash

# init workspace
cd ~
mkdir ws
cd ws
mrt ws init
mrt pkg add $1
mrt catkin build --release
